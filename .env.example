# Offline v0.1.0 needs no credentials. These are reserved parent-service inputs.
# Never use service-role credentials, signing secrets or child data in this file.
SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
CONTENT_CATALOGUE_URL=
# Supply signing credentials outside Git through protected CI secrets when needed.
